{
  "name": "hooks",
  "scope": "umijs",
  "builds": [
    {
      "src": "now-build.sh",
      "use": "@now/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/$1" }]
}