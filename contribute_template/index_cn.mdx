---
name: yourname
route: /yourname
menu: '分类'
edit: false
sidebar: true
---

# 标题

对该 Hook 的一些介绍，包括该 Hook 的常见应用场景等等。

**核心特性**

* 罗列该 Hook 的核心特性

## 代码演示

此处为由简到繁的代码示例，尽量包含所有的参数。

## API
```
用法 show，类似

const result: T = useSearch<T>(
  asyncFn: (value: any) => Promise<T>,
  options?: Options,
);
```

### result

| 参数 | 说明     | 类型 |
|------|----------|------|
| data | 文字说明 | any  |

### 参数

| 参数    | 说明     | 类型                   | 默认值 |
|---------|----------|------------------------|--------|
| asyncFn | 文字说明 | (value: any)=> Promise | -      |


### Options

| 参数 | 说明     | 类型   | 默认值 |
|------|----------|--------|--------|
| wait | 文字说明 | number | 300    |

## 备注（如果有）

一些对该 Hook 的补充讲解

## FAQ（如果有）

### Q
A