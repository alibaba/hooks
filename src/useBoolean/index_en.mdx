---
name: useBoolean
route: /useBoolean
menu: 'Basic Hooks'
edit: false
sidebar: true
---
import JackBox from 'jackbox';

import Demo1 from './demo/demo1';
import Demo1CodeTsx from '!raw-loader!./demo/demo1.tsx';
import Demo1CodeJsx from '!raw-loader!./demo/demo1.jsx';

# useBoolean

React state hook that tracks value of a boolean.

**Core Characteristics**

* Set default state.
* Toggles value of a boolean.

## Examples

<JackBox jsCode={Demo1CodeJsx} tsCode={Demo1CodeTsx} demoName='Basic usage' description='Default as a switch function,or accept a parameter to change state'>
  <Demo1 />
</JackBox>

## API

```javascript
const [state, toggle] = useToggle();

const [state, toggle] = useToggle(
  defaultValue?: boolean,
);
```

### Result

| Property | Description                                         | Type                 |
|----------|--------------------------------------|----------------------|
| state  | State value                         | boolean              |
| toggle | Trigger state change, accept an optional parameter | (value?: any) => void |

### Params

| Property | Description                                 | Type                   | Default |
|---------|----------------------------------------------|------------------------|--------|
| defaultValue | Optional，set a default value  | boolean \| undefined | -      |